@model IEnumerable<EquitiesIntegration.Models.Citizenship>

@{
    ViewBag.Title = "Citizenships";
}

<h2>Citizenships</h2>

<p>
    @Html.ActionLink("Create New Citizenship", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CitizenshipId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CitizenshipId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CitizenshipId })
        </td>
    </tr>
}

</table>
